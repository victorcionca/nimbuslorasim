class NodeConfigTemplate():

    @classmethod
    def configure(self, nodes, gws, area, channel_model):
        """
        For each node, sets a configuration directly in the object.
        channel_model is provided.
        Doesn't have a return code.
        Params
        nodes       -- List of nodes
        gws         -- List of gateways
        area        -- Area object for determining area bounds
        channel_model.
        """
        pass
